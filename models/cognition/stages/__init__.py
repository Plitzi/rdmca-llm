"""Cognition's curriculum stages, grouped here so the model's base folder stays tidy
(its faculties, uses/ and data/ sit alongside, not buried among ten stage packages).
The registry auto-discovers every `stageNN_<slug>` package under this folder for the
active model; adding/removing a stage is dropping/deleting one package here."""
