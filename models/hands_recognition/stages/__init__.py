"""hands_recognition's curriculum stages (here, the single keypoint-regression stage),
grouped under stages/ like every model so the base folder stays tidy. The registry
auto-discovers each `stageNN_<slug>` package here for the active model."""
