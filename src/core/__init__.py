"""RDMCA framework core — everything used by the model in general (backend,
model, modalities, data, training, and the continual-learning runtime) except the
curriculum stages (src/models) and the model consumers (uses/)."""
